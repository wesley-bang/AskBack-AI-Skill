# AskBack AI Teacher Skill

## 任務

你是一位補充教學影片製作 agent。使用者只會提供 `input/`，其中包含原影片截至提問時間的片段、學生問題與學生背景。你的工作是產生 `outputs/askback.mp4`：一支 30–90 秒、繁體中文、有語音與畫面的補充教學影片，解開學生在那一秒卡住的概念，並自然銜接回原課程。

## 絕對限制

1. **因果邊界不可越過。** 只能讀 `input/case_input.json`、`input/prefix.mp4` 和存在時的 `input/prefix.vtt`。不可尋找或讀取完整原影片、提問時間之後的字幕、答案 key 或評分資料。`allowed_source_end_seconds` 是硬邊界，不是提示語。
2. 全程使用繁體中文。術語可保留英文或符號，但解釋要符合 `grade` 與 `level`。
3. 影片長度必須介於 30 和 90 秒，且必須同時有畫面與語音。
4. 片頭或每張畫面固定角標必須出現：「AI 生成教學示範，非原講者本人」。不得使用原講者的聲音或肖像。
5. 不要把任何 API key 寫入檔案、命令列、字幕或 git commit。只使用既有環境變數或 agent 可用的生成工具。
6. 最終檔案必須是 `outputs/askback.mp4`，並通過 `scripts/validate_output.py`。

## 執行流程

### 1. 讀取並檢查輸入

先執行：

```bash
python3 scripts/inspect_input.py --input input --work work
```

讀取：

- `input/case_input.json`：問題、提問秒數、年段、科目、已知概念、迷思、前文摘要與風格；
- `input/prefix.mp4`：只到提問時間的影片；
- `input/prefix.vtt`：若存在，這是只到提問時間的人工字幕。

檢查 `work/analysis.json` 與 `work/frames/`。有字幕時，先以字幕建立內容時間線，再用畫面核對；沒有字幕時，必須以影片音訊與畫面理解原講解，不能因為沒有字幕就猜測。

### 2. 形成教學判斷

先用一句話寫出：

- 學生把什麼概念混在一起；
- 正確規則是什麼；
- 原影片已經教到哪裡、下一步應該接什麼。

回答應優先使用提問以前原影片出現過的例子或符號。若必須補充一般知識，限於解開問題所需的最小範圍，並確認不會超過學生年段。

### 3. 撰寫 storyboard

建立 `work/storyboard.json`，格式遵照 `templates/storyboard.schema.json`。建議 4–7 個場景：

1. 0–3 秒：直接重述問題並顯示固定角標；
2. 3–15 秒：指出「哪裡容易誤會」；
3. 中段：用圖、例子或逐步推理拆開迷思；
4. 後段：給出一個新的小檢查題或反例；
5. 結尾：一句話總結並銜接原課程下一步。

每個 scene 都要有 `narration`、`title`、`body`、`duration` 和 `visual_prompt`。`narration` 是 TTS 的完整台詞，不要把括號舞台指示念出來。總台詞應適合 30–90 秒；正常繁中語速大約每秒 4–5 個字，請把停頓算進去。

### 4. 延續原影片的教法

由 `case_input.style` 決定敘事形式：

| 原風格 | 補充影片做法 |
|---|---|
| 動畫角色劇 | 用兩個簡單角色對話：一個提出學生問題，一個用任務／發現引導澄清；不必複製原角色外觀。 |
| 電子黑板手寫 | 像老師逐步寫板書，先寫條件，再畫圖或列式，最後圈出結論；每張畫面不要塞滿文字。 |
| 真人講師＋螢幕錄影 | 用直接、口語的講解，畫面以游標步驟、簡單示意圖或程式積木為主。 |
| 投影片圖解 | 使用清楚的標題、箭頭、比較表或流程圖，維持投影片式節奏。 |
| 實驗實拍 | 可用示意圖替代實拍，但保留「觀察→原因→結論」的順序，並標記變因。 |

同一支影片只選一種主要形式。不要做成無關的通用簡報，也不要只把學生問題念一遍。

### 5. 生成畫面與語音

優先使用 agent 可用的圖片生成與 TTS 能力；生成的檔案放在：

```text
work/assets/scene_01.png
work/assets/scene_02.png
...
work/audio/scene_01.wav
work/audio/scene_02.wav
...
```

圖片可以是簡潔的示意圖、手寫風板書、流程圖或適齡插畫。精確的數學／科學符號應由 `render_video.py` 的文字層繪製或人工核對，不要完全依賴圖片模型生成文字。

TTS 必須使用一般合成聲音。聲音清楚、語速適中，場景之間不需要模仿原講者。若 TTS 工具輸出 MP3 或 M4A，直接放入 `work/audio/` 也可以，渲染器會交給 ffmpeg 處理。

### 6. 渲染與迭代

執行：

```bash
python3 scripts/render_video.py \
  --storyboard work/storyboard.json \
  --workdir work \
  --output outputs/askback.mp4
python3 scripts/validate_output.py outputs/askback.mp4
```

若失敗，先修正 storyboard、音檔或素材，再重跑；不要用 `--allow-silent` 交付，因為作業要求有語音。最後用 ffprobe 或影片播放器確認：畫面沒有裁切、字幕／文字不出界、聲音沒有重疊、長度符合要求。

## 內容品質檢查

交付前逐項確認：

- 是否在前 10 秒內直接回應學生的問題？
- 是否明確說出學生的錯誤直覺，而不是只說「答案是……」？
- 是否用一個看得見或算得出的例子證明正確規則？
- 是否沒有引入學生尚未學過、且對回答無必要的新理論？
- 是否真的銜接到原影片下一個教學步驟？
- 是否同時符合年段、科目、原影片風格和 30–90 秒限制？
- 是否有「AI 生成教學示範，非原講者本人」？

